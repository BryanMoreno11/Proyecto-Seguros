import { SeguroVehiculoPipe } from './seguro-vehiculo.pipe';

describe('SeguroVehiculoPipe', () => {
  it('create an instance', () => {
    const pipe = new SeguroVehiculoPipe();
    expect(pipe).toBeTruthy();
  });
});
