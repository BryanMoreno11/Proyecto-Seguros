import { SeguroVidaPipe } from './seguro-vida.pipe';

describe('SeguroVidaPipe', () => {
  it('create an instance', () => {
    const pipe = new SeguroVidaPipe();
    expect(pipe).toBeTruthy();
  });
});
