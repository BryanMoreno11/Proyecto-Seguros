import { AseguradoraVidaPipe } from './aseguradora-vida.pipe';

describe('AseguradoraVidaPipe', () => {
  it('create an instance', () => {
    const pipe = new AseguradoraVidaPipe();
    expect(pipe).toBeTruthy();
  });
});
